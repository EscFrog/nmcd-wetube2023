extends ../base
include ../mixins/video

block content
  each data in user.videos
    //- 아래 코드는 video.pug 파일에 mixin이라는 키워드로 미리 정의해 놓은 videoToMixin 블록에 데이터를 전달해준다.
    +videoToMixin(data)
  else
    li Sorry nothing found.